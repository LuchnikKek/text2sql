| дата | симптом | корневая причина | правило на будущее |
|---|---|---|---|
| 2026-08-07 | все id источника отдают 404, хотя данные есть | ключи мок-словаря стали `int`, а `entity_id` из пути всегда `str` — `.get()` не совпадает | при смене типа id в источнике приводить `entity_id` к типу ключа внутри `fetch()` (нераспарсенный id → `EntityNotFound`, не 500) и синхронно обновлять id в тестах |
| 2026-08-07 | `/chat` с `chat_id: "123"` отдаёт 422, хотя по ТЗ эндпоинт «всегда 200» | `chat_id` — `UUID`, невалидное значение отсекает валидация FastAPI до хендлера; правило «всегда 200» покрывает только ошибки агента внутри `try/except` | `chat_id`/`message_id` везде UUID — слать валидный UUID; 422 на невалидное тело не баг и не чинить (тот же класс, что урок про типы id выше) |
| 2026-08-07 | `events` отдаёт 404 на существующий `entity_id` после смены id со строк на числа | буквальное повторение урока №1: ключи `_EVENTS` стали `int`, `entity_id` из пути остался `str` | правило урока №1 применяется ко ВСЕМ модулям `app/enrichment/`, а не только к правленому: меняя тип id в одном источнике, повтори приведение типа в `fetch()` и обнови id в тестах и README |
