# Чек-лист приёмки: источник обогащения добавлен

Шаг считается «готово», только когда выполнены **все** пункты.
Проверять по факту (запустить, посмотреть вывод), а не по памяти.

## 1. Тесты зелёные

- [ ] `uv run pytest -q` — 0 failed. Вывод показан пользователю.
- [ ] Новые тесты действительно новые: покрывают удачный `fetch()`,
      `EntityNotFound` на неизвестном id, эндпоинт
      `GET /api/v1/enrich/<name>/{entity_id}` → 200 с ожидаемым `data`.
- [ ] Существующие тесты не правились ради зелени. Если пришлось —
      это сигнал, что сломан контракт: остановиться и сказать об этом.
- [ ] Тест, регистрирующий источник вручную, убирает его через
      `unregister()` в teardown (иначе следующий тест упадёт на
      «already registered»).

## 2. Источник виден в реестре

- [ ] `register(<Name>Source())` вызван внизу модуля источника (импорт
      модуля делает автоимпорт пакета, руками его добавлять не нужно).
- [ ] Тест-сторож `test_every_source_module_registers_a_source` зелёный.
- [ ] Модуль не импортирует сам пакет `app.enrichment` (только `.base` /
      `.registry`) — иначе циклический импорт при автоимпорте.
- [ ] `uv run python -c "import app.enrichment as e; print(e.source_names())"`
      выводит новое имя.
- [ ] Имя уникально: старт приложения не падает на
      `ValueError: Enrichment source already registered`.
- [ ] `isinstance(NewSource(), EnrichmentSource)` — True (совпадение по
      протоколу: атрибут `name` + `async def fetch(entity_id) -> dict`).

## 3. README обновлён

- [ ] В README перечислен новый источник: имя, что отдаёт, пример
      `entity_id`. Если раздела про источники обогащения ещё нет —
      завести его и заодно внести `/api/v1/enrich/{source}/{entity_id}`
      в таблицу эндпоинтов.
- [ ] Если источник ходит во внешнюю систему — в таблице источников
      названы его клиент и env-переменные (`*_CLIENT_URL`,
      `*_CLIENT_TIMEOUT`) и то, что без настроенного URL он отдаёт 502.

## 4. Контракты и границы не задеты

- [ ] `EnrichResponse` (`app/schemas.py`) не менялся: наружу по-прежнему
      `source` / `entity_id` / `data`.
- [ ] `app/routers/enrich.py`, `base.py`, `registry.py` не правились —
      добавление источника не должно их трогать.
- [ ] Новых зависимостей не появилось без явного согласования.
- [ ] Если источник ходит наружу — HTTP спрятан в клиенте `app/clients`
      (подкласс `RestClient` + свой `*ClientSettings` с `env_prefix`),
      источник только переводит `RestClientNotFound`/`RestClientError`
      в `EntityNotFound`/`EnrichmentError`.
- [ ] Клиент не импортирует `app.enrichment` — слой остаётся переиспользуемым.
- [ ] Секретов и хардкоженных URL в модулях нет — только настройки клиента и env.

## 5. Финальная проверка

- [ ] Выполнен skill `/verify` (или как минимум `uv run pytest -q`),
      результат показан пользователю.
